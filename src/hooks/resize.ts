import React, { useEffect, useState } from 'react';

const useResize = () => {

        // const onResize = () => {
        //     waitForFinalEvent(() => { }, 100);
        // }

        // window && window.addEventListener('resize', onResize);

    return [];
}

export default useResize;